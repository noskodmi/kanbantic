# @kanbantic/ui

Tailwind v4 baseline + shadcn-style primitive helpers.

Phase 0 ships:

- `cn` classname utility (`clsx` + `tailwind-merge`)
- `styles.css` Tailwind v4 entry (theme tokens)

Phase 3 lands `Button`, `Card`, `Input`, `Dialog`, `Tabs` shadcn components.
