{
  "name": "@kanbantic/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir=dist",
    "dev": "wrangler dev",
    "lint": "eslint src",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "migrate:local": "for f in migrations/*.sql; do wrangler d1 execute kanbantic-indexer --local --file \"$f\" || exit 1; done",
    "migrate:remote": "for f in migrations/*.sql; do wrangler d1 execute kanbantic-indexer --remote --file \"$f\" || exit 1; done"
  },
  "dependencies": {
    "@kanbantic/shared": "workspace:*",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.34",
    "@cloudflare/workers-types": "^4.20241224.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "wrangler": "^3.99.0"
  }
}
